<h1>Seattle Grace Hospital - Patients </h1>

<ul>
  <% @patients.each do |patient| %>
    <li><%= link_to "#{patient.name} - #{pluralize(patient.appt_count, "appointment")}", patient_path(patient)%></li>
  <% end %>
</ul>
