<h1>Seattle Grace Hospital - Patients </h1>

<ul>
  <% @patients.each do |patient| %>
    <li><%= link_to "#{patient.name} - #{pluralize("appointments", patient.appt_count)}"%></li>
  <% end %>
</ul>
